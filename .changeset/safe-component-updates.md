---
"zunoui": minor
---

Add read-only `diff` and guarded `update` commands for copied components. Record source fingerprints in `zuno.lock.json` and refuse updates when local edits or missing baselines prevent safe replacement. Document the package, existing-theme setup and update workflow.
