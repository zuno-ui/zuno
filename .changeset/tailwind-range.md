---
"zunoui": patch
---

`init` accepts any Tailwind CSS v4 range, such as `^4`, and existing dependencies at any compatible version of the one ZUNO pins (for example `tailwind-merge@^3.6.0` when ZUNO pins `3.3.1`), including in monorepos where packages are hoisted.
