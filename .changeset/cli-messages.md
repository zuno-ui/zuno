---
"zunoui": patch
---

Clearer CLI messages and package README. When a component file already exists, the conflict error now suggests setting `aliases.ui` to `@/components/zuno` to install ZUNO alongside another library.
